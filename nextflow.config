profiles {

  local {
    executor.name = "local"
  }

  small {
    executor.name = "slurm"
    process.memory = "5G"
  }

  medium {
    executor.name = "slurm"
    process.memory = "30G"
  }

  large {
    executor.name = "slurm"
    executor.memory = "128G"
  }
}
